# レガシーデータ取り込みの運用終了メモ（2026-02-17）

## 結論
- 既存のレガシーデータ取り込みは完了。
- 取り込み機能（`src/backend/02-1_BusinessLogic_Batch.js`）は当面保持する。

## 保持する理由
- 取り込み漏れ・監査対応など、将来の再取込がゼロとは言い切れないため。
- 既存コードは通常運用を壊さず、緊急時の復旧手段として有効なため。

## 今後の扱い
- 新規開発対象にはしない（原則メンテナンス凍結）。
- 仕様変更や大規模リファクタは実施しない。
- 不具合で通常運用に影響が出る場合のみ最小修正する。

## PR55 レビュー指摘について
- 未対応項目（モジュール分割など）は、現時点では `won't fix` とする。
- 参照資料は `docs/archive/completed-tasks/2026-02-17-PR55-REVIEW.md` に保存。
