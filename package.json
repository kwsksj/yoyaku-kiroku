{
  "name": "yoyaku-kiroku",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "types:check": "npm run types:check:backend && npm run types:check:frontend",
    "types:check:backend": "tsc --project tsconfig.check.backend.json",
    "types:check:frontend": "tsc --project tsconfig.check.frontend.json",
    "types:clean": "rm -rf types/generated-backend-globals types/generated-frontend-globals types/generated-shared-globals",
    "types:generate": "npm run types:clean && npm run types:generate:shared && npm run types:generate:backend && npm run types:generate:frontend && npm run types:generate:bridge && npm run types:generate:index",
    "types:generate:shared": "tsc --project tsconfig.shared.dts.json",
    "types:generate:backend": "tsc --project tsconfig.backend.dts.json",
    "types:generate:frontend": "tsc --project tsconfig.frontend.dts.json",
    "types:generate:index": "node tools/create-dts-index.js",
    "types:generate:bridge": "node tools/create-global-bridge.js",
    "types:refresh": "npm run types:generate && npm run types:check",
    "format": "npm run format:fix",
    "format:fix": "prettier --write \"**/*.{js,html,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,html,json,md}\"",
    "lint:check": "eslint . --ext .js --ignore-pattern '*.d.ts'",
    "lint:fix": "eslint . --ext .js --fix --ignore-pattern '*.d.ts'",
    "lint:md:check": "markdownlint \"**/*.md\"",
    "lint:md:fix": "markdownlint \"**/*.md\" --fix",
    "check": "npm run format:check && npm run lint:check && npm run types:refresh",
    "fix": "npm run format:fix && npm run lint:fix && npm run types:refresh",
    "build": "npm run check && node tools/unified-build.js build",
    "build:force": "node tools/unified-build.js build",
    "watch": "node tools/unified-build.js watch",
    "switch:env": "node tools/switch-env.js",
    "push:prod": "npm run switch:env -- prod && clasp push --force",
    "push:test": "npm run switch:env -- test && clasp push --force",
    "deploy:prod": "npm run switch:env -- prod && clasp push --force && clasp deploy",
    "deploy:test": "npm run switch:env -- test && clasp push --force && clasp deploy",
    "open:dev:prod": "npm run switch:env -- prod && clasp push --force && node tools/open-dev-url.js prod",
    "open:dev:test": "npm run switch:env -- test && clasp push --force && node tools/open-dev-url.js test",
    "dev:test": "npm run switch:env -- test && npm run build && clasp push --force",
    "dev:prod": "npm run switch:env -- prod && npm run build && clasp push --force",
    "dev:open:test": "npm run switch:env -- test && npm run build && clasp push --force && node tools/open-dev-url.js test",
    "dev:open:prod": "npm run switch:env -- prod && npm run build && clasp push --force && node tools/open-dev-url.js prod"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kwsksj/yoyaku-kiroku.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kwsksj/yoyaku-kiroku/issues"
  },
  "homepage": "https://github.com/kwsksj/yoyaku-kiroku#readme",
  "description": "",
  "devDependencies": {
    "@google/clasp": "^3.0.6-alpha",
    "@types/google-apps-script": "^1.0.99",
    "chokidar": "^4.0.0",
    "eslint": "^9.33.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-googleappsscript": "^1.0.5",
    "globals": "^16.3.0",
    "markdownlint-cli": "^0.45.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
